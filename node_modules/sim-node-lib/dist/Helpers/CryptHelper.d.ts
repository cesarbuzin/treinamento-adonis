export declare class CryptHelper {
    static encrypt(mensagem: string): string;
    static decrypt(mensagem: string): string;
    static AESEncrypt(text: string): Promise<string>;
    static AESDecrypt(text: string): Promise<string>;
}
